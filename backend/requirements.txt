fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
pydantic-settings>=2.2
# [requests] extra 필수 — id_token.verify_oauth2_token이 requests 트랜스포트를 쓴다.
# 빠뜨리면 런타임에 ImportError가 나는데, 로컬엔 requests가 이미 깔려 있어 드러나지 않는다.
google-auth[requests]>=2.29
itsdangerous>=2.2
