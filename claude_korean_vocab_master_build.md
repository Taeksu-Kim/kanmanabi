# 한국어 학습용 어휘 마스터 데이터 구축 지시

## 목적

국립국어원 공식 데이터를 사용해 한국어 학습용 어휘 마스터 데이터를 만든다.

최종적으로는 각 어휘에 아래 정보를 포함한다.

```json
{
  "level": 1,
  "word": "가격",
  "homonym_no": 2,
  "pos": "명사",
  "guide": "가격이 비싸다",
  "ja": ["価格", "値段"]
}
```

최종 서비스에서는 원본 파일을 직접 사용하지 않고, 한 번 가공한 `korean_vocab_master.json` 또는 동등한 형태의 단일 파일만 사용한다.

---

## 입력 데이터

### 1. 국제 통용 한국어 표준 교육과정 XLSX

파일 예시:

```text
korean_standard_vocab_grammar.xlsx
```

필요 데이터:

- 등급: 1~6급
- 어휘
- 품사
- 길잡이말
- 동음이의어 번호

예:

```text
1급 | 가격02 | 명사 | 가격이 비싸다
1급 | 가다01 | 동사 | 학교에 가다
```

`가격02`, `가다01`의 뒤 숫자는 동음이의어 구분 번호로 분리해서 저장한다.

예:

```text
가격02
↓
word = 가격
homonym_no = 2
```

---

### 2. 한국어기초사전 전체 다운로드 데이터

API를 반복 호출하지 않고 한국어기초사전 전체 다운로드 파일을 로컬에서 사용한다.

가능하면 JSON 또는 XML을 우선 사용하고, Excel만 제공되는 경우 Excel을 사용한다.

필요 데이터:

- 한국어 표제어
- 동음이의어 번호
- 품사
- 일본어 대역

일본어 대역은 가능한 경우 짧은 대표 번역어 위주로 사용한다.

예:

```text
가격
동음이의어 번호: 2
품사: 명사
일본어: 価格、値段
```

---

## 처리 흐름

```text
국제 통용 한국어 표준 교육과정 XLSX
        +
한국어기초사전 전체 다운로드 데이터
        ↓
로컬 파싱 및 정규화
        ↓
word + homonym_no + pos 기준 JOIN
        ↓
일본어 대역 매핑
        ↓
매칭 실패/중복 항목 별도 추출
        ↓
korean_vocab_master.json 생성
```

---

## JOIN 기준

가능하면 아래 순서로 매칭한다.

### 1차

```text
word + homonym_no + pos
```

### 2차

동음이의어 번호가 없거나 데이터 형식이 다른 경우:

```text
word + pos
```

단, 동일한 `word + pos` 후보가 여러 개인 경우 자동 확정하지 않는다.

### 3차

모호한 항목은 `guide`를 참고해 의미를 판별한다.

필요한 경우 로컬에서 실행 가능한 LLM을 보조적으로 사용할 수 있다.

LLM은 다음 용도로만 사용한다.

- 다중 후보 중 의미가 맞는 항목 선택
- 매칭 실패 원인 분석
- 일본어 대역이 비어 있는 일부 항목 보완

기본 일본어 데이터는 한국어기초사전의 공식 일본어 대역을 우선한다.

---

## 정규화 규칙

### 한국어 표제어

```text
가격02 → 가격
가다01 → 가다
```

숫자는 `homonym_no`로 분리한다.

### homonym_no

값이 없으면 `null`.

```json
{
  "word": "가게",
  "homonym_no": null
}
```

### 일본어

최종 데이터에서는 배열 형태를 권장한다.

```json
{
  "ja": ["価格", "値段"]
}
```

사전에 한 문자열로 들어 있는 경우 적절히 분리할 수 있으면 분리한다.

단, 의미를 훼손할 가능성이 있으면 원문 문자열 하나를 배열 요소 하나로 저장한다.

---

## 최종 데이터 스키마

```json
{
  "level": 1,
  "word": "가격",
  "homonym_no": 2,
  "pos": "명사",
  "guide": "가격이 비싸다",
  "ja": ["価格", "値段"]
}
```

필수 필드:

| 필드 | 설명 |
|---|---|
| `level` | 국제 통용 한국어 표준 교육과정 1~6급 |
| `word` | 한국어 표제어 |
| `homonym_no` | 동음이의어 번호, 없으면 null |
| `pos` | 품사 |
| `guide` | 국립국어원 길잡이말 |
| `ja` | 일본어 대표 뜻 배열 |

---

## 결과 파일

최소 아래 2개를 생성한다.

### 1. 정상 매칭 결과

```text
korean_vocab_master.json
```

가능하면 전체 10,635개 어휘를 포함한다.

### 2. 검토 필요 항목

```text
korean_vocab_unmatched.json
```

포함 대상:

- 일본어 대역을 찾지 못한 항목
- 후보가 2개 이상이라 확정할 수 없는 항목
- 품사 불일치
- 동음이의어 번호 불일치
- 기타 비정상 데이터

예:

```json
{
  "level": 2,
  "word": "example",
  "homonym_no": null,
  "pos": "명사",
  "guide": "...",
  "reason": "multiple_dictionary_candidates"
}
```

---

## 검증

작업 완료 후 반드시 아래 통계를 출력한다.

```text
교육과정 전체 어휘 수
정상 매칭 수
매칭 실패 수
다중 후보 수
일본어 대역 없는 수
등급별 매칭 수
```

목표는 10,635개 중 자동 매칭률을 최대한 높이는 것이다.

모호한 항목을 억지로 자동 매칭하지 말고 `korean_vocab_unmatched.json`으로 보내는 것을 우선한다.

---

## 라이선스 처리

사용 데이터는 국립국어원/공공데이터포털의 공식 공개 데이터를 기준으로 한다.

프로젝트 내 README 또는 데이터 출처 문서에 최소 다음 내용을 남긴다.

```text
출처:
- 국립국어원 「국제 통용 한국어 표준 교육과정」
- 국립국어원 「한국어기초사전」
```

공공데이터포털의 한국어기초사전 Open API는 공공누리 제1유형(출처표시)으로 안내되어 있으므로, 실제 배포 시 현재 공식 이용조건을 다시 확인하고 출처를 표시한다.

---

## 구현 원칙

- API를 10,635번 호출하는 방식은 사용하지 않는다.
- 가능한 한 전체 다운로드 파일을 로컬에서 한 번 파싱한다.
- JOIN 로직은 재실행 가능하게 만든다.
- 원본 파일을 수정하지 않는다.
- 최종 서비스에서는 `korean_vocab_master.json`만 사용 가능하도록 한다.
- 애매한 항목은 추측해서 넣지 말고 검토 목록으로 분리한다.
